Необходимо реализовать консольное приложение, которое:

1)  Читает данные из входного файла
+ Читает txt, xml, json.
+ Читает zip любой вложенности и с паролями. 
+ Читает зашифрованные файлы (алгоритм AES, если это алгоритм...).
+ Может показать все файлы в папке, если ввести её имя.
2)  Обрабатывает полученную информацию
+ Парсер на рекурсивной функции (любая сложность)
- Парсер на "классической" обратной польской записи
+- Парсер на Regex (не обрабатывает скобки)
+ Парсер на сторонней библиотеке
3)  Записывает данные в выходной файл
+ txt, xml, json по выбору пользователя (не связано с входным файлом)
- Не архивирует
- Не шифрует

4) Приложение реализовать двумя способами: без использования Design Patterns и c использованием Design Patterns 
(Decorator и Builder, можно оформить Builder в виде Singleton-а)
 
+ Decorator сделан в виде классов CryptoReader (объединяет Txt/Xml/JsonReader) и TheReader (объединяет Crypto/Archive/FolderReader) 
- Builder и Singleton на текущем этапе задания "по идее" не применимы
 
Следующие шаги по задаче:
1)  Добавить UI:
+ Консольный;
- Используя любую графическую библиотеку Java;
2)  Реализовать логику как Web Service:
- Rest, используя любой Java engine;
- SOAP, используя любой Java engine;
3)  Соединить все вместе UI и Web Service;
- Не сделано

+ Присутствует полноценная обработка исключений. Падение программы я самостоятельно вызвать не смог
- Тестов нет